Create a commit with the current changes describing what you did and then push it to git so we don't lose the work.  If
there are any extra files, prompt the user to either add them to the current commit as well or to abort the process due
to corrupted work.
