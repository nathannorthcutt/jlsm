# architecture / feature-footprints

> Condensed records of completed features: what was built, what domains were touched, what was learned. Generated during `/feature-retro`.

| Subject | File | Description | Added |
|---------|------|-------------|-------|
| f04-obligation-resolution--wd-03 | [f04-obligation-resolution--wd-03.md](f04-obligation-resolution--wd-03.md) | First end-to-end cluster scatter-gather: QueryRequestPayload format + QueryRequestHandler dispatcher + parallel scatter via virtual-thread executor (F04.R68 + R77) | 2026-04-20 |
| implement-sstable-enhancements--wd-01 | [implement-sstable-enhancements--wd-01.md](implement-sstable-enhancements--wd-01.md) | Byte-budget block cache displaces entry-count LRU; `sstable.byte-budget-block-cache` v4 APPROVED + `sstable.striped-block-cache` v4 amended in-place; aggressive adversarial pipeline (spec-author Pass 2+3 → harden → audit) | 2026-04-21 |
| implement-sstable-enhancements--wd-02 | [implement-sstable-enhancements--wd-02.md](implement-sstable-enhancements--wd-02.md) | Pool-aware SSTable block size: `TrieSSTableWriter.Builder.pool(ArenaBufferPool)` derives block size from pool buffer size; `sstable.pool-aware-block-size` v5 APPROVED; audit hardened `ArenaBufferPool.close()` to two-phase CAS with outstanding-acquire ISE guard | 2026-04-22 |
| implement-sstable-enhancements--wd-03 | [implement-sstable-enhancements--wd-03.md](implement-sstable-enhancements--wd-03.md) | SSTable v5 end-to-end integrity: per-block + per-section + footer CRC32C, magic-first dispatch, recovery scan, atomic commit with `<final>.partial.<uuid>` + `ATOMIC_MOVE` + content-addressed fallback, reader `FAILED` terminal state; `sstable.end-to-end-integrity` v5 DRAFT; 20 audit findings all fixed | 2026-04-22 |
| implement-encryption-lifecycle--wd-01 | [implement-encryption-lifecycle--wd-01.md](implement-encryption-lifecycle--wd-01.md) | Three-tier key hierarchy foundation (root KEK → domain KEK → DEK) for multi-tenant encryption: 28 new constructs + 1 rename across `jlsm.encryption`, `jlsm.encryption.internal`, `jlsm.encryption.local`; `KmsClient` SPI + `LocalKmsClient`; HKDF + AES-KWP + AES-GCM primitives with canonical-AAD `EncryptionContext`; sharded per-tenant key registry with atomic commit; `encryption.primitives-lifecycle` v8 APPROVED → v9 DRAFT; `/audit` 52 fixes of 95 findings; 581 tests | 2026-04-23 |
| implement-encryption-lifecycle--wd-02 | [implement-encryption-lifecycle--wd-02.md](implement-encryption-lifecycle--wd-02.md) | Ciphertext envelope (4B BE DEK-version prefix) + SSTable v6 footer scope signalling for multi-tenant DEK locating + cross-scope rejection: 10 new constructs (`TableScope`, `ReadContext`, `EncryptionMetadata`, `IdentifierValidator`, `V6Footer`, `EnvelopeCodec`, `CatalogLock`, `FileBasedCatalogLock`, `CatalogIndex`, `WriterCommitHook` SPI + `EngineWriterCommitHook`) + sealed `Table` with two permits + 5 specs amended (`sstable.footer-encryption-scope` v5→v9; `encryption.ciphertext-envelope` v1→v2; `engine.catalog-operations` v2→v3; `engine.clustered-table-construction` v1; `encryption.primitives-lifecycle` v9→v10); 4 ADRs (incl. first exercise of `pre-ga-format-deprecation-policy`); `/audit` 21 fixes of 45 findings + 5 RELAX→/spec-author; 205 tests | 2026-04-25 |
