---
id: WD-03
title: Cache
group: decisions-backlog
status: READY
domains: [decisions]
artifact_deps: []
produces:
  - { type: adr, slug: "atomic-cross-stripe-eviction" }
  - { type: adr, slug: "parallel-large-cache-eviction" }
---

## Summary

Performance infrastructure — cache eviction refinements for the striped block
cache. Self-contained within jlsm-core. Low priority; current behavior is
acceptable at current scale. Can run in parallel with other clusters.

Decisions in this cluster: atomic-cross-stripe-eviction, parallel-large-cache-eviction

## Acceptance Criteria

- Each decision slug in this cluster has an accepted ADR or is explicitly
  closed/re-deferred
- Implementation notes recorded for any that need follow-up features

## Implementation Notes

**Effort classification:**
- Gap-fills: (none)
- Minor features: atomic-cross-stripe-eviction, parallel-large-cache-eviction
- Full features: (none)

**Approach:** Both are minor features extending the existing cross-stripe-eviction
architecture. Brief /architect evaluations. May be closed if current behavior
proves sufficient at scale.
