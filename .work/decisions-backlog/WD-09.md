---
id: WD-09
title: Encryption & Security
group: decisions-backlog
status: READY
domains: [decisions]
artifact_deps: []
produces:
  - { type: adr, slug: "pre-encrypted-flag-persistence" }
  - { type: adr, slug: "per-field-pre-encryption" }
  - { type: adr, slug: "per-field-key-binding" }
  - { type: adr, slug: "encryption-key-rotation" }
  - { type: adr, slug: "wal-entry-encryption" }
  - { type: adr, slug: "unencrypted-to-encrypted-migration" }
  - { type: adr, slug: "client-side-encryption-sdk" }
  - { type: adr, slug: "encrypted-prefix-wildcard-queries" }
  - { type: adr, slug: "encrypted-fuzzy-matching" }
  - { type: adr, slug: "encrypted-cross-field-joins" }
  - { type: adr, slug: "index-access-pattern-leakage" }
---

## Summary

Field-level encryption lifecycle — key rotation, WAL encryption, migration,
and advanced encrypted query capabilities. Minor features can proceed in
parallel with networking work. Full features (encrypted query types) require
research on searchable encryption before /architect.

Decisions in this cluster: pre-encrypted-flag-persistence, per-field-pre-encryption,
per-field-key-binding, encryption-key-rotation, wal-entry-encryption,
unencrypted-to-encrypted-migration, client-side-encryption-sdk,
encrypted-prefix-wildcard-queries, encrypted-fuzzy-matching,
encrypted-cross-field-joins, index-access-pattern-leakage

## Acceptance Criteria

- Each decision slug in this cluster has an accepted ADR or is explicitly
  closed/re-deferred
- Implementation notes recorded for any that need follow-up features

## Implementation Notes

**Effort classification:**
- Gap-fills: pre-encrypted-flag-persistence
- Minor features: per-field-pre-encryption, per-field-key-binding,
  encryption-key-rotation, wal-entry-encryption
- Full features: unencrypted-to-encrypted-migration, client-side-encryption-sdk,
  encrypted-prefix-wildcard-queries, encrypted-fuzzy-matching,
  encrypted-cross-field-joins, index-access-pattern-leakage

**Approach:** Gap-fill first (flag persistence). Minor features next — these
extend field-encryption-api-design and pre-encrypted-document-signaling.
encrypted-fuzzy-matching and encrypted-prefix-wildcard-queries both depend on
encryption-key-rotation — resolve it before those. Full features need research:
/research "searchable encryption for prefix and fuzzy queries" before /architect.
client-side-encryption-sdk depends on per-field-pre-encryption and encryption-key-rotation.
