---
id: WD-02
title: Storage & Compression
group: decisions-backlog
status: COMPLETE
domains: [decisions]
artifact_deps: []
produces:
  - { type: adr, slug: "automatic-backend-detection" }
  - { type: adr, slug: "block-cache-block-size-interaction" }
  - { type: adr, slug: "sstable-end-to-end-integrity" }
  - { type: adr, slug: "memorysegment-codec-api" }
  - { type: adr, slug: "pure-java-lz4-codec" }
  - { type: adr, slug: "wal-group-commit" }
  - { type: adr, slug: "cross-sst-dictionary-sharing" }
  - { type: adr, slug: "wal-dictionary-compression" }
  - { type: adr, slug: "corruption-repair-recovery" }
  - { type: adr, slug: "pure-java-zstd-compressor" }
---

## Summary

Foundational I/O layer — codec contracts, block integrity, dictionary sharing,
and write-path performance. Phase 1 gap-fills and Phase 2 compression decisions
are complete. Remaining items extend the compression, dictionary, and integrity
story. Several are performance-gated (evaluate only when benchmarks warrant).

Decisions in this cluster: automatic-backend-detection, block-cache-block-size-interaction,
sstable-end-to-end-integrity, memorysegment-codec-api, pure-java-lz4-codec,
wal-group-commit, cross-sst-dictionary-sharing, wal-dictionary-compression,
corruption-repair-recovery, pure-java-zstd-compressor

## Acceptance Criteria

- Each decision slug in this cluster has an accepted ADR or is explicitly
  closed/re-deferred
- Implementation notes recorded for any that need follow-up features

## Implementation Notes

**Effort classification:**
- Gap-fills: automatic-backend-detection, block-cache-block-size-interaction
- Minor features: sstable-end-to-end-integrity, memorysegment-codec-api,
  pure-java-lz4-codec, wal-group-commit, cross-sst-dictionary-sharing,
  wal-dictionary-compression
- Full features: corruption-repair-recovery, pure-java-zstd-compressor

**Approach:** Process gap-fills first. Minor features next — several are
performance-gated (pure-java-lz4-codec, wal-group-commit, cross-sst-dictionary-sharing,
wal-dictionary-compression) and may be closed or re-deferred if benchmarks don't
warrant them. corruption-repair-recovery needs replication context (Cluster 7).
pure-java-zstd-compressor needs native dependency to become a deployment problem.
