---
id: WD-08
title: Query Execution
group: decisions-backlog
status: SPECIFIED
domains: [decisions]
artifact_deps: []
produces:
  - { type: adr, slug: "aggregation-query-merge" }
  - { type: adr, slug: "limit-offset-pushdown" }
  - { type: adr, slug: "distributed-join-execution" }
---

## Summary

Distributed query — aggregation merges, pagination pushdown, and joins.
Queries route through the partition layer, so Cluster 7 (Partitioning) must
be stable first. distributed-join-execution needs research before /architect.

Decisions in this cluster: aggregation-query-merge, limit-offset-pushdown,
distributed-join-execution

## Acceptance Criteria

- Each decision slug in this cluster has an accepted ADR or is explicitly
  closed/re-deferred
- Implementation notes recorded for any that need follow-up features

## Implementation Notes

**Effort classification:**
- Gap-fills: (none)
- Minor features: aggregation-query-merge, limit-offset-pushdown
- Full features: distributed-join-execution

**Approach:** Minor features first (aggregation merge strategies, LIMIT/OFFSET
pushdown). distributed-join-execution is the most complex — needs research on
co-partitioned joins, broadcast joins, and shuffle joins before /architect.
distributed-join-execution depends on both aggregation-query-merge and
limit-offset-pushdown being resolved.
