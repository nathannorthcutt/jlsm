---
id: WD-01
title: Key hierarchy (root KEK, data KEKs, DEKs)
group: implement-encryption-lifecycle
status: COMPLETE
domains: [encryption]
artifact_deps: []
produces: []
---

## Summary

Three-level key hierarchy: root KEK → data-domain KEKs → per-table or per-object DEKs. Define storage, derivation, and scoping rules. This is a foundational WD — no other WD can complete without the hierarchy types landing.

## Acceptance Criteria

- encryption.primitives-lifecycle key-hierarchy requirements direct-annotated
- Test: DEK derived from KEK + context is reproducible and context-scoped
- Test: KEK rotation does not orphan DEKs (wrapping preserved)
- Interface contract published for downstream consumers
