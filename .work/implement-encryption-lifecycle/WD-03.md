---
id: WD-03
title: DEK lifecycle + KEK rotation
group: implement-encryption-lifecycle
status: IMPLEMENTING
domains: [encryption]
artifact_deps:
  - { type: wd, ref: "WD-01", required_state: COMPLETE }
  - { type: wd, ref: "WD-02", required_state: COMPLETE }
produces: []
---

## Summary

DEK generation, wrapping under KEK, rotation policy, and revocation flow. KEK rotation re-wraps existing DEKs without re-encrypting payloads (for performance).

## Acceptance Criteria

- encryption.primitives-lifecycle DEK-lifecycle requirements direct-annotated
- Test: KEK rotation produces new-wrapped DEKs; old DEKs still decrypt existing data
- Test: DEK revocation path (future reads fail, no silent success)
