---
id: WD-02
title: Ciphertext format + signalling
group: implement-encryption-lifecycle
status: DRAFT
domains: [encryption]
artifact_deps:
  - { type: wd, ref: "WD-01", required_state: COMPLETE }
produces: []
---

## Summary

Encoded ciphertext envelope — algorithm tag, key identifier, nonce, MAC, and payload layout. Signalling mechanism for the reader to locate which DEK was used.

## Acceptance Criteria

- encryption.primitives-lifecycle ciphertext-format requirements direct-annotated
- Round-trip test: encrypt/decrypt via the envelope format across algorithm variants
- Interface-contract spec published for cross-domain consumption
