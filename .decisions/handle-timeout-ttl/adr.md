---
problem: "handle-timeout-ttl"
date: "2026-03-30"
version: 1
status: "deferred"
---

# Handle Timeout/TTL — Deferred

## Problem
Automatic handle timeout/TTL — could be added later as a decorator.

## Why Deferred
Scoped out during `engine-api-surface-design` decision. Could be added later as a decorator.

## Resume When
When `engine-api-surface-design` implementation is stable and this concern becomes blocking.

## What Is Known So Far
See `.decisions/engine-api-surface-design/adr.md` for the architectural context that excluded this concern.

## Next Step
Run `/architect "handle-timeout-ttl"` when ready to evaluate.
